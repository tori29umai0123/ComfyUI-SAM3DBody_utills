# ComfyUI-SAM3DBody Requirements
#
# Heavy Python / native dependencies (bpy, trimesh, transformers,
# huggingface-hub, torch, scipy's friends, etc.) are declared in
# `nodes/comfy-env.toml` and installed into an isolated environment
# by `comfy-env`. Only the lightweight bootstrap packages needed to
# load THIS file and run the dispatcher sit in requirements.txt.

# Pinned to 0.1.75 because 0.1.76+ calls ``Path.is_junction()``
# unconditionally on Windows (isolation/wrap.py:208), which only exists
# on Python 3.12+. ComfyUI's bundled venv ships Python 3.11 so that call
# raises AttributeError and aborts node loading. 0.1.75 is the last
# release without the offending check.
comfy-env==0.1.75
comfy-3d-viewers==0.2.33
msvc-runtime; sys_platform == "win32"

# Core (also redeclared in comfy-env.toml)
numpy>=1.21.0,<2.0.0
pillow>=9.0.0
opencv-python-headless>=4.8.0
